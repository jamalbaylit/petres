from __future__ import annotations

"""PyVista 3-D layer rendering functions.

Provides internal helpers for adding geological layers—wells, zones,
corner-point grids, and horizon surfaces—to a PyVista plotter backend.
All public symbols are private (prefixed with ``_``) and are intended
for use by :class:`petres.viewers.viewer3d.pyvista.viewer.PyVista3DViewer`
only.
"""
