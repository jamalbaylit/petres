version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"
  jobs:
    install:
      - pip install uv
      - uv sync --group docs
    build:
      html:
        - cd docs && make html BUILDDIR=$READTHEDOCS_OUTPUT