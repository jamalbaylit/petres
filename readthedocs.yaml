version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"
  jobs:
    install:
      - pip install uv
      - uv sync --group docs
    build:
      html:
        - cd docs && make dirhtml BUILDDIR=$READTHEDOCS_OUTPUT